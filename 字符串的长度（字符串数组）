#include<stdio.h>
#include<string.h>

int main()
{
    char s[1010];
    int i,n,j,a;
    scanf("%d",&n);
    getchar();//去掉回车
    for(i=1;i<=n;i++)
    {
        gets(s);
        a=strlen(s);//读取字符串的长度
        printf("case %d:length=",i);
        printf("%d.\n",a);
    }

}
