#include<stdio.h>
#include<string.h>
//字符串逆序
int main()
{
    char s[110];
    int i,a;
    scanf("%s",s);//输入字符串数组时不需要取地址符&
    a=strlen(s);//获得字符串长度
    for(i=a-1;i>=0;i--)
        printf("%c",s[i]);//这时逆序输出时是单个字符输出，所以得把输出转换设为字符‘%c’
}
