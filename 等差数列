#include<stdio.h>
int main()
{
    int a,b,c;
    scanf("start = %d, step = %d, end = %d",&a,&b,&c);
    if(b>0)
   {
    while(a<=c)
    {
        printf("%d ",a);
        a=a+b;
    }
   }
    else if(b<0)
    {
       while(a>=c)
    {
        printf("%d ",a);
        a=a+b;
    }
    }
}
